import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fabiannestor.focustimer',
  appName: 'Focus Timer',
  webDir: 'dist'
};

export default config;
